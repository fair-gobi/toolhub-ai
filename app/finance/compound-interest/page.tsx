'use client'
import { useState } from 'react'
const C = ['NPR','INR','USD','EUR','GBP','JPY','AUD','CAD']
export default function CI(){const [p,setP]=useState(100000),[r,setR]=useState(8),[t,setT]=useState(5),[n,setN]=useState(1),[c,setC]=useState('NPR');const A=p*Math.pow(1+r/100/n,n*t);const fmt=(v:number)=>new Intl.NumberFormat('en',{style:'currency',currency:c,maximumFractionDigits:0}).format(v);return(<main className="p-6 max-w-2xl mx-auto"><h1 className="text-2xl font-bold mb-4">Compound Interest</h1><div className="bg-white p-6 rounded-2xl border space-y-3"><select value={c} onChange={e=>setC(e.target.value)} className="w-full p-2 border rounded">{C.map(x=><option key={x}>{x}</option>)}</select><input type="number" value={p} onChange={e=>setP(+e.target.value)} placeholder="Principal" className="w-full p-3 border rounded"/><input type="number" value={r} onChange={e=>setR(+e.target.value)} placeholder="Rate %" className="w-full p-3 border rounded"/><input type="number" value={t} onChange={e=>setT(+e.target.value)} placeholder="Years" className="w-full p-3 border rounded"/><select value={n} onChange={e=>setN(+e.target.value)} className="w-full p-2 border rounded"><option value={1}>Yearly</option><option value={4}>Quarterly</option><option value={12}>Monthly</option></select></div><div className="mt-4 bg-blue-50 p-6 rounded-2xl text-center"><div className="text-sm">Maturity Amount</div><div className="text-3xl font-bold text-blue-600">{fmt(A)}</div><div className="text-sm mt-2">Interest: {fmt(A-p)}</div></div></main>)}
