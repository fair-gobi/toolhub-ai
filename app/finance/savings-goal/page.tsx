'use client'
import {useState} from 'react'
const C=['NPR','INR','USD']
export default function SG(){const[t,setT]=useState(1000000),[y,setY]=useState(5),[r,setR]=useState(8),[c,setC]=useState('NPR');const mr=r/100/12,n=y*12,pmt=t*mr/((Math.pow(1+mr,n)-1)*(1+mr));const fmt=(v:number)=>new Intl.NumberFormat('en',{style:'currency',currency:c,maximumFractionDigits:0}).format(v);return(<main className="p-6 max-w-2xl mx-auto"><h1 className="text-2xl font-bold mb-4">Savings Goal</h1><div className="bg-white p-6 rounded-2xl border space-y-3"><select value={c} onChange={e=>setC(e.target.value)} className="w-full p-2 border rounded">{C.map(x=><option key={x}>{x}</option>)}</select><input type="number" value={t} onChange={e=>setT(+e.target.value)} placeholder="Target Amount" className="w-full p-3 border rounded"/><input type="number" value={y} onChange={e=>setY(+e.target.value)} placeholder="Years" className="w-full p-3 border rounded"/><input type="number" value={r} onChange={e=>setR(+e.target.value)} placeholder="Expected Return %" className="w-full p-3 border rounded"/></div><div className="mt-4 bg-orange-50 p-6 rounded-2xl text-center"><div>Monthly SIP Needed</div><div className="text-3xl font-bold text-orange-600">{fmt(pmt)}</div></div></main>)}
